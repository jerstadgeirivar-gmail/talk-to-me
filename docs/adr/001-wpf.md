# ADR 001: WPF

Use C#/.NET 10 WPF because the product is Windows-only and requires native HWND, UI Automation, tray, hotkey, clipboard, and input integration. The UI driver validates real WPF controls through FlaUI UIA3.
